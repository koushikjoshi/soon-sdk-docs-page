# SOONClient

The `SOONClient` class serves as the core interface for connecting to the Solana network.

## Methods
- **getConnection()**: Returns the connection to the Solana network.
- **getLatestBlock()**: Fetches the latest confirmed block slot.
- **getTransaction(txSignature)**: Retrieves details of a transaction using its signature.
